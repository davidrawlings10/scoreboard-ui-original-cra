// import React from "React";
// import Button as MuiButton, { ButtonProps } from "@material-ui/core/Button";

/*const Button = (props: ButtonProps) => {
  return <MuiButton {...props}></MuiButton>;
};

export default Button;
*/

export default 5;
