import { useState } from "react";

import config from "../config";
import Season from "../Entity/Season";

export default function SeasonCache() {
  /*const [seasonList, setSeasonList] = useState<Array<Season>>();
  function getSeason(id: number) {
    return !!seasonList ? seasonList : fetchSeason;
  }

  function fetchSeason() {
    fetch(config.baseUrl + "/season/getSeasons")
      .then((res) => res.json())
      .then((seasonsResult) => {
        setSeasonList(seasonsResult.list);
      });
  }*/
}
